; PHP defaults (2M/8M) are far too small for object uploads — match the
; nginx deploy option's client_max_body_size (docker/nginx/default.conf).
upload_max_filesize = 5120M
post_max_size = 5120M
memory_limit = 512M
max_execution_time = 300
